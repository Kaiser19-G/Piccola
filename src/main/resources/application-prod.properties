# Production Configuration

# Server Configuration
server.port=${PORT:8080}

# Database Configuration for Railway MySQL
# Usar el host público de Railway para conexiones externas desde Render
spring.datasource.url=${DATABASE_URL:jdbc:mysql://${MYSQLHOST:yamanote.proxy.rlwy.net}:${MYSQLPORT:28411}/${MYSQLDATABASE:railway}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:pFdxybzAvYZDjdvsIyURmKakkWKycbQE}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:valentinohernandez27@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:eblj rrbc unzc vegr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging Configuration
logging.level.com.example.demo=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:WARN}
